# AI Entry

Start here before working on this repository.

## Read Order
1. Read `quick-context.md` first.
2. Read `project-analysis.md` only if more detail is needed.
3. Read specific source files only when implementation detail is required.
4. Do not scan the entire repository unless explicitly necessary.

## Purpose
This repository contains AI context files to reduce repeated repository analysis and token usage.

## Rules for AI Agents
- Treat `quick-context.md` as the fast project summary.
- Treat `project-analysis.md` as the deeper architecture reference.
- Keep both files updated when architecture, entry points, workflows, or major dependencies change.
- Prefer targeted file reads over broad repo scans.
- Do not rely on stale context if source files clearly contradict these docs.
